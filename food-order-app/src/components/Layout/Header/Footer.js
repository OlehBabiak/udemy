import { Fragment } from "react";
import classes from './Footer.module.css'

const Footer = () => {
	return (
		<Fragment>
			<footer className={ classes.footer }>
				<p>Designed with the hope of getting into ELIFTECH courses :) </p>
			</footer>
		</Fragment>
	);
}

export default Footer
